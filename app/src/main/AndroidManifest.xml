<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.electiva.envido32.appsaenzpena">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".SettingsActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".CandidatosActivity" />
        <activity android:name=".CandidatoInfoActivity" />
        <activity android:name=".NewCandidatoActivity" />
        <activity android:name=".DeleteCandidatoActivity" />
        <activity android:name=".PadronActivity" />
        <activity android:name=".EscrutineoActivity" />
        <activity android:name=".DistritoActivity"></activity>
    </application>

</manifest>